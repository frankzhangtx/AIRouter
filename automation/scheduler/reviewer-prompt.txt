Use $scheduled-quality-reviewer with NEXT_REVIEWING. This is a
non-interactive, read-only scheduled run. Resolve a task only through the
deterministic selector. If zero or multiple REVIEWING tasks exist, stop
cleanly. Inspect the contract and diff, then submit one evidence-backed
decision promptly; the submission script owns fresh deterministic verification.
Do not spend the decision step budget browsing generated reports. Never edit,
push, or merge.
