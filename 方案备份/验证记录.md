# 备份验证记录

## 基线检查

- 检查时间：2026-08-19（Asia/Shanghai）。
- 当前分支：`feature-opencode-scheduler`。
- 当前提交：`829693652e3737ad94c7cc75214b09fb2b58715b`。
- 备份前 `git status --short --branch` 只显示分支跟踪信息，没有修改项。
- `git worktree list --porcelain` 只显示
  `/Users/zhanglong/files/program/cctest`，没有遗留任务 worktree。
- `.git/automation-runtime/locks/` 为空。
- 三个 runtime state 均为 `COMPLETED`。

## 自动化测试

执行：

```bash
./scripts/automation/tests/run-tests.sh
```

结果：`1..38`，38 项全部通过，退出码 `0`。覆盖范围包括：

- V3 配置和合同校验；
- 三个人工批准 token 拒绝/接受；
- 持久仓库租约和并发排斥；
- 默认原地任务分支和可选单 worktree；
- RED、范围门、G1–G6 和 Reviewer handoff；
- Reviewer-only 恢复及有界修复；
- SHA 绑定验收卡和 diff 篡改拒绝；
- 计划/合同与产品改动的单一组合提交；
- 原分支漂移阻断；
- 成功后的任务分支删除；
- 中止归档及无 planning-only commit；
- no-push 不变量。

执行：

```bash
./scripts/automation/shadow-run.sh
```

结果：退出码 `0`，摘要为：

```json
{
  "enabled": true,
  "mode": "orchestrated",
  "activeContracts": 5,
  "runtimeStates": 3,
  "mutationPerformed": false
}
```

## 原文件与快照一致性

以下目录均执行了 `diff -qr`，没有差异：

- `.opencode/agents`
- `.opencode/commands`
- `.opencode/skills`
- `automation`
- `scripts/automation`
- `docs/plans`
- `.git/automation-runtime`

根级文件、OpenCode package/lock 和三个 HTML 指南均通过原样 `cp -p`
复制。两份 `.gitignore` 仅在备份目录中改名，其文件内容未修改。

## 快照摘要

摘要算法：进入每个树根，对 `find . -type f` 的相对路径按 `LC_ALL=C`
排序，逐文件计算 SHA-256，再对完整的“文件摘要 + 相对路径”列表计算一次
SHA-256。

| 树 | 文件数 | 大小 | SHA-256 |
| --- | ---: | ---: | --- |
| `snapshot/repository` | 70 | 660 KiB | `e08bb576db26b4b85db79d044c4146bd372266fd51da2e5aa039f52a82af23ca` |
| `external-dependencies` | 183 | 2508 KiB | `ecb94b3cd106fa906ddb1c87aac4db927cb26c7a642fef7a2817c7a88cb22017` |
| `runtime-audit` | 79 | 764 KiB | `8284a8804177ad84f71616667d8f415ea97ef82314aad8fdad26b7b203fc22b7` |

可随时运行 `./方案备份/verify-backup.sh` 重算并核对这些摘要。
